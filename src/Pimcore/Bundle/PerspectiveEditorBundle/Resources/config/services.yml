services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: true
        bind:
            $configDirectory: "%kernel.root_dir%/config/pimcore/"

    AdminBundle\Controller\:
        resource: '../../Controller'
        tags: ['controller.service_arguments']

    AdminBundle\Services\:
        resource: '../../Services'